page << "Status.hide()"
page << "Dialog.close()"

page.replace_html("edit-link-#{@tmpl.id}", @tmpl.name)
page << "$('#edit-link-#{@tmpl.id}').effect('pulsate')"

# remove old preview (which is kinda cached), so that it can be reloaded again
page.replace("template-preview-#{@tmpl.id}", "")
page << "$('#template-checkbox-#{@tmpl.id}').attr('checked', false)"