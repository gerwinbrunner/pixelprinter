page.replace_html "modal-dialog", :partial => "preview", :locals => {:rendered_template => @rendered_template, :tmpl => @tmpl}
page << %Q[
  $('#modal-dialog').dialog('destroy');
  $('#modal-dialog').dialog(Dialog.options({title: "#{@tmpl.name} &mdash; Preview"}))
]