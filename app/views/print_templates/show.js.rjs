page.replace_html "modal-dialog", :partial => 'preview', :locals => {:tmpl => @tmpl, :order => @order}
page << "Dialog.close()"
page << "Dialog.open('#{@tmpl.name} &mdash; Preview')"
page << "Status.hide()"