page.replace_html "modal-dialog", :partial => 'orders/preview', :locals => {:tmpl => @tmpl, :order => @order, :tmpls => @tmpls}
page << "Dialog.close();"
page << "Dialog.open('#{@tmpl.name} &mdash; Preview');"